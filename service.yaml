name: foundation-stereo-service
version: 0.1.0
description: Zero-shot stereo depth estimation using FoundationStereo (CVPR 2025) on GPU

requires:
  gpu: true
  vram_gb: 8

deploy:
  image: tidybot/foundation-stereo-service:0.1.0
  port: 8003
  health: /health
  ready_timeout: 120

client: client.py
